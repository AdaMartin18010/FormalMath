# 同伦类型论：类型论基础推导推理树

## 概述

本推理树展示Martin-Löf类型论的基础构造，以及如何通过同论解释将类型视为空间、相等性视为路径，为同伦类型论奠定基础。

## 推理树

```mermaid
graph TD
    A[类型论基础] --> B[简单类型论]
    B --> B1[基础类型]<-->B2[ℕ, Bool, 1, 0]
    B --> B3[函数类型]<-->B4[A → B]
    B --> B5[积与和]<-->B6[A × B, A + B]
    
    B --> C[依赖类型]
    C --> C1[依赖积]<-->C2[Πx:A.Bx]
    C --> C3[依赖和]<-->C4[Σx:A.Bx]
    C --> C5[同族类型]<-->C6[B: A → Type]
    
    A --> D[恒等类型]
    D --> D1[Martin-Löf恒等]<-->D2[x =_A y]
    D --> D3[构造子refl]<-->D4[自反性证明]
    D --> D5[归纳原理J]<-->D6[基于路径的归纳]
    
    D --> E[同论解释]
    E --> E1[类型↔空间]<-->E2[A作为拓扑空间]
    E --> E3[项↔点]<-->E4[a: A对应点a ∈ A]
    
    E --> F[相等性↔路径]
    F --> F1[p: x = y]<-->F2[从x到y的路径]
    F --> F3[refl ↔ 常值路径]<-->F4[id_x]
    F --> F5[p⁻¹ ↔ 逆路径]<-->F6[路径反转]
    F --> F7[p·q ↔ 路径复合]<-->F8[连接路径]
    
    G[高阶结构] --> G1[路径的路径]<-->G2[x = y = z]
    G --> G3[ΩA - 环路空间]<-->G4[a =_A a]
    G --> G5[高阶群结构]<-->G6[π_n的同伦群]
    
    H[规则体系] --> H1[构造]<-->H2[引入/消去/计算]
    H --> H3[β归约]<-->H4[计算规则]
    H --> H5[η扩张]<-->H6[唯一性原理]
    
    I[归纳类型] --> I1[归纳定义]<-->I2[ℕ, List, Tree]
    I --> I3[消去子]<-->I4[rec, ind]
    I --> I5[初始代数语义]<-->I6[F-代数的初始对象]
    
    style A fill:#f9f,stroke:#333,stroke-width:2px
    style B fill:#bbf,stroke:#333
    style C fill:#bfb,stroke:#333
    style D fill:#f96,stroke:#333
    style E fill:#fbb,stroke:#333
    style F fill:#fdd,stroke:#333
    style G fill:#fcc,stroke:#333
    style H fill:#f99,stroke:#333
    style I fill:#f88,stroke:#333
```

## 核心构造详解

### 1. 依赖类型

**依赖积**（全称量词）：
```
Π(x:A). B(x)   对应   ∀x:A, B(x)
```

**依赖和**（存在量词）：
```
Σ(x:A). B(x)   对应   {(x,b) | x:A, b:B(x)}
```

### 2. 恒等类型规则

**形成规则**：
```
Γ ⊢ A: Type    Γ ⊢ a: A    Γ ⊢ b: A
-----------------------------------
      Γ ⊢ Id_A(a,b): Type
```

**引入规则**：
```
Γ ⊢ a: A
--------------
Γ ⊢ refl_a: Id_A(a,a)
```

**消去规则（J）**：
```
C: Πx,y:A. (x=y) → Type    c: Πx:A. C(x,x,refl_x)
------------------------------------------------
        J(C,c): Πx,y:A. Πp:x=y. C(x,y,p)
```

### 3. 同论解释

| 类型论 | 拓扑学 | 说明 |
|--------|--------|------|
| 类型A | 空间\|A\| | 拓扑空间 |
| 项a:A | 点a ∈ \|A\| | 空间的点 |
| p: a = b | 路径p: [0,1]→\|A\| | 从a到b的连续路径 |
| α: p = q | 同伦α: p ≃ q | 路径之间的形变 |
| 路径复合 | 路径连接 | 拓扑路径乘法 |

### 4. 高阶结构

**环路空间**：
```
Ω(A,a) := (a =_A a)
```

**迭代环路空间**：
```
Ωⁿ(A,a) 对应 π_n(\|A\|, a)
```

## 示例：自然数类型

```
inductive ℕ: Type
| zero: ℕ
| succ: ℕ → ℕ
```

消去规则（原始递归）：
```
rec_ℕ(C, c_0, c_s, zero) ≡ c_0
rec_ℕ(C, c_0, c_s, succ(n)) ≡ c_s(n, rec_ℕ(C, c_0, c_s, n))
```

---
*生成时间: 2026年4月*
*领域: 类型论 / 数学基础 / 同伦论*
