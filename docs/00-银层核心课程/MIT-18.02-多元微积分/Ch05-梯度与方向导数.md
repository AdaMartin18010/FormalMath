---
title: "Ch05 梯度与方向导数"
level: silver
course: MIT 18.02 多元微积分
msc_primary: 26

  - 26B12
target_courses:
  - MIT 18.02
status: completed
created_at: 2026-04-18
---

# Ch05 梯度与方向导数

## 1. 梯度向量

**定义 1.1（梯度）**。函数 $f(x, y, z)$ 的**梯度（gradient）**定义为：

$$\nabla f = \left(\frac{\partial f}{\partial x}, \frac{\partial f}{\partial y}, \frac{\partial f}{\partial z}\right)$$

**定理 1.2（梯度的几何意义）**。$\nabla f$ 指向 $f$ 增长最快的方向，其模 $|\nabla f|$ 等于该方向的方向导数。

*证明*：方向导数 $D_{\mathbf{u}} f = \nabla f \cdot \mathbf{u} = |\nabla f| \cos\theta$。当 $\mathbf{u}$ 与 $\nabla f$ 同向时（$\theta = 0$），方向导数最大，为 $|\nabla f|$。$\square$

**推论 1.3**。梯度方向垂直于等值面（或等值线）$f(x, y, z) = c$。

## 2. 方向导数

**定义 2.1（方向导数）**。设 $\mathbf{u}$ 为单位向量，$f$ 在 $\mathbf{u}$ 方向的**方向导数**为：

$$D_{\mathbf{u}} f = \nabla f \cdot \mathbf{u}$$

**定理 2.2**。方向导数的最大值等于 $|\nabla f|$，最小值等于 $-|\nabla f|$，分别在 $\mathbf{u} = \frac{\nabla f}{|\nabla f|}$ 和 $\mathbf{u} = -\frac{\nabla f}{|\nabla f|}$ 方向取得。

## 3. 典型例题

**例题 3.1**。设 $f(x, y) = x^2 + 2y^2$，求在点 $(1, 1)$ 处沿方向 $(3, 4)$ 的方向导数。

*解答*：$\nabla f = (2x, 4y)$，$\nabla f(1,1) = (2, 4)$，$\mathbf{u} = (\frac{3}{5}, \frac{4}{5})$，$D_{\mathbf{u}}f = 2 \cdot \frac{3}{5} + 4 \cdot \frac{4}{5} = \frac{22}{5}$。

**例题 3.2**。求函数 $f(x, y, z) = xyz$ 在点 $(1, 2, 3)$ 处增长最快的方向及增长率。

*解答*：$\nabla f = (yz, xz, xy)$，$\nabla f(1,2,3) = (6, 3, 2)$。增长最快方向为 $(6, 3, 2)$，增长率 $|\nabla f| = \sqrt{36+9+4} = 7$。

## 4. 练习题

1. 求 $f(x, y, z) = x^2 + y^2 + z^2$ 在点 $(1, 1, 1)$ 处沿 $(1, 2, 2)$ 方向的方向导数。
2. 证明：若 $f$ 可微且 $|\nabla f| \leq M$，则 $|f(P) - f(Q)| \leq M|PQ|$。
3. 求曲面 $x^2 + y^2 + z^2 = 3$ 在点 $(1, 1, 1)$ 处的切平面方程。

## 5. Lean4 形式化对照

```lean4
import Mathlib

-- 梯度
example (f : EuclideanSpace ℝ (Fin 3) → ℝ) (hf : Differentiable ℝ f) (p : EuclideanSpace ℝ (Fin 3)) :
    EuclideanSpace ℝ (Fin 3) :=
  fderiv ℝ f p 1

-- 方向导数
example (f : EuclideanSpace ℝ (Fin 3) → ℝ) (hf : Differentiable ℝ f)
    (p u : EuclideanSpace ℝ (Fin 3)) (hu : ‖u‖ = 1) : ℝ :=
  fderiv ℝ f p u
```

## 6. 应用

### 6.1 最速下降法

在优化中，最速下降法利用负梯度方向来最小化函数：$\mathbf{x}_{new} = \mathbf{x}_{old} - \alpha \nabla f(\mathbf{x}_{old})$。

### 6.2 地形分析

梯度方向指向海拔上升最快的方向，垂直于等高线。这是地理信息系统（GIS）中的基础概念。

---

## 习题

**习题 5.1**。设 $f(x, y) = x^2 y + e^{xy}$，求在点 $(1, 0)$ 处的梯度，以及沿方向 $\mathbf{u} = (\frac{3}{5}, \frac{4}{5})$ 的方向导数。

*解答*：$\nabla f = (2xy + ye^{xy}, x^2 + xe^{xy})$，$\nabla f(1,0) = (0, 2)$。
$D_{\mathbf{u}} f = \nabla f \cdot \mathbf{u} = 0 \cdot \frac{3}{5} + 2 \cdot \frac{4}{5} = \frac{8}{5}$。$\square$

---

**习题 5.2**。求函数 $f(x,y) = x^2 + xy + y^2$ 在点 $(1,1)$ 处方向导数的最大值及对应方向。

*解答*：$\nabla f = (2x+y, x+2y)$，$\nabla f(1,1) = (3, 3)$。
方向导数最大值 $= |\nabla f| = \sqrt{18} = 3\sqrt{2}$，方向为 $\frac{1}{\sqrt{2}}(1, 1)$。$\square$

---

**习题 5.3**。证明：若 $f$ 可微，则 $f$ 沿梯度方向增长最快，沿负梯度方向下降最快。

*解答*：$D_{\mathbf{u}} f = \nabla f \cdot \mathbf{u} = |\nabla f||\mathbf{u}|\cos\theta = |\nabla f|\cos\theta$（$|\mathbf{u}|=1$）。
当 $\theta = 0$（即 $\mathbf{u}$ 与 $\nabla f$ 同向）时取最大值 $|\nabla f|$；
当 $\theta = \pi$ 时取最小值 $-|\nabla f|$。$\square$

**参考文献**

1. Stewart, J. *Multivariable Calculus*. Cengage Learning, 2015.
2. Marsden, J. E. & Tromba, A. J. *Vector Calculus*. W. H. Freeman, 2011.
