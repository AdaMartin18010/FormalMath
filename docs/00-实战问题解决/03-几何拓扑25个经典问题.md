# 几何拓扑25个经典问题实战指南

> **目标**：掌握几何与拓扑学核心问题的系统化解决流程
> **参考**：Hatcher《代数拓扑》、do Carmo《曲线与曲面的微分几何》
> **适用范围**：本科高年级至研究生初级阶段

---

## 目录

1. [类型1：流形构造问题（6个）](#类型1流形构造问题)
2. [类型2：曲率计算问题（7个）](#类型2曲率计算问题)
3. [类型3：同调计算问题（6个）](#类型3同调计算问题)
4. [类型4：基本群问题（6个）](#类型4基本群问题)

---

## 类型1：流形构造问题

### 问题1：球面 S^n 的标准光滑结构验证

#### 问题陈述

验证 n 维球面 S^n = {x ∈ ℝ^(n+1) : |x| = 1} 配备标准坐标卡后构成光滑流形。

#### 问题类型识别

| 维度 | 类型 | 核心任务 |
|:---:|:---|:---|
| 对象类型 | 光滑流形 | 验证流形公理 |
| 方法类型 | 坐标卡构造 | 建立图册并验证相容性 |
| 难度等级 | ⭐⭐ | 基础验证类问题 |

#### 方法选择决策

```
┌─────────────────────────────────────────────────────────┐
│  问题：验证 S^n 的光滑结构                                 │
│  ↓                                                       │
│  选择：标准球极投影坐标卡                                  │
│  ↓                                                       │
│  北半球卡：φ_N: S^n\{N} → R^n                            │
│  南半球卡：φ_S: S^n\{S} → R^n                            │
│  ↓                                                       │
│  验证：φ_S ∘ φ_N^(-1) 是光滑微分同胚                      │
└─────────────────────────────────────────────────────────┘
```

#### 完整解决流程

**Step 1：坐标卡定义**

取北极 N = (0, ..., 0, 1) 和南极 S = (0, ..., 0, -1)。

**北半球坐标卡** (U_N, φ_N)：
- U_N = S^n \\ {N}
- 球极投影：φ_N(x_1, ..., x_{n+1}) = (1/(1-x_{n+1}))(x_1, ..., x_n)

**南半球坐标卡** (U_S, φ_S)：
- U_S = S^n \\ {S}
- 球极投影：φ_S(x_1, ..., x_{n+1}) = (1/(1+x_{n+1}))(x_1, ..., x_n)

**Step 2：覆盖性验证**

显然 U_N ∪ U_S = S^n，因为 S^n 上每点至少不等于北极或南极之一。

**Step 3：转移映射计算**

设 (y_1, ..., y_n) = φ_N(x)，则转移映射为：
φ_S ∘ φ_N^(-1)(y) = y/|y|^2

这是 ℝ^n \\ {0} 上的反演变换，显然是光滑的。

**Step 4：Hausdorff与第二可数**

S^n 作为 ℝ^(n+1) 的子空间，继承 Hausdorff 和第二可数性质。

#### 验证与反思

**正确性验证：**
- ✅ 转移映射的光滑性：反演变换 y ↦ y/|y|^2 在 ℝ^n \\ {0} 上无限可微
- ✅ 图册覆盖性：两个开集覆盖整个球面
- ✅ 拓扑性质：子空间拓扑保持

**方法反思：**
> 球极投影是球面几何中的标准工具。关键观察：球极投影将"无穷远"映射到对径点，转移映射的反演特性反映了这一几何直观。

#### 相关变体

| 变体 | 问题 | 方法调整 |
|:---:|:---|:---|
| 实射影空间 | ℝP^n 的光滑结构 | 使用齐次坐标卡 |
| 复射影空间 | ℂP^n 的复结构 | 全纯坐标卡 |
| 一般超曲面 | f^(-1)(0) 的正则值原像 | 隐函数定理 |

---

### 问题2：积流形 S^1 × S^1（环面）的切丛构造

#### 问题陈述

构造环面 T^2 = S^1 × S^1 的切丛 TT^2，证明其平凡性。

#### 问题类型识别

| 维度 | 类型 | 核心任务 |
|:---:|:---|:---|
| 对象类型 | 积流形 | 切丛构造与性质 |
| 方法类型 | 直积构造 | 利用积流形结构 |
| 难度等级 | ⭐⭐⭐ | 中等构造类问题 |

#### 方法选择决策

```
┌─────────────────────────────────────────────────────────┐
│  问题：T^2 的切丛平凡性                                    │
│  ↓                                                       │
│  策略：利用 T(T^2) ≅ T(S^1) × T(S^1)                     │
│  ↓                                                       │
│  关键：S^1 的切丛 TS^1 ≅ S^1 × R 是平凡的                  │
│  ↓                                                       │
│  结论：T^2 是平行化流形                                    │
└─────────────────────────────────────────────────────────┘
```

#### 完整解决流程

**Step 1：环面的参数化**

环面可参数化为：
Φ(θ, φ) = ((R + r·cos θ)·cos φ, (R + r·cos θ)·sin φ, r·sin θ)

其中 R > r > 0，θ, φ ∈ [0, 2π)。

**Step 2：切空间计算**

在点 p = Φ(θ_0, φ_0) 处，切空间由偏导数张成：
∂Φ/∂θ = (-r·sin θ_0·cos φ_0, -r·sin θ_0·sin φ_0, r·cos θ_0)
∂Φ/∂φ = (-(R+r·cos θ_0)·sin φ_0, (R+r·cos θ_0)·cos φ_0, 0)

**Step 3：切丛构造**

利用积流形性质：
T(T^2) = T(S^1 × S^1) ≅ TS^1 × TS^1 ≅ (S^1 × ℝ) × (S^1 × ℝ) ≅ T^2 × ℝ^2

**Step 4：平凡性证明**

构造全局标架场：
- X_1 = ∂/∂θ（经线方向）
- X_2 = ∂/∂φ（纬线方向）

这两个向量场处处线性无关，构成切丛的全局平凡化。

#### 验证与反思

**正确性验证：**
- ✅ 切空间维度：dim T_pT^2 = 2 = dim T^2 ✓
- ✅ 向量场光滑性：参数化 Φ 是光滑的，偏导数也是光滑的
- ✅ 线性无关性：∂Φ/∂θ × ∂Φ/∂φ ≠ 0

**方法反思：**
> 环面的切丛平凡性反映了其"平坦"的拓扑性质。这一结论可推广：n 维环面 T^n 都是平行化的。但注意：S^2 的切丛不平凡（hairy ball theorem 的深层原因）。

#### 相关变体

| 变体 | 问题 | 关键区别 |
|:---:|:---|:---|
| 高维环面 | T^n 的切丛 | 类似的平凡性 |
| 球面 S^2 | TS^2 是否平凡 | 不平凡，需要示性类证明 |
| 李群 | 任何李群的切丛 | 总是平凡的 |

---

### 问题3：莫比乌斯带的光滑向量场构造

#### 问题陈述

构造莫比乌斯带上的光滑向量场，使其恰有一个零点，并分析该向量场的拓扑指数。

#### 问题类型识别

| 维度 | 类型 | 核心任务 |
|:---:|:---|:---|
| 对象类型 | 非定向流形 | 向量场构造 |
| 方法类型 | 局部拼接 | 利用商结构 |
| 难度等级 | ⭐⭐⭐⭐ | 高级构造类问题 |

#### 方法选择决策

```
┌─────────────────────────────────────────────────────────┐
│  问题：莫比乌斯带上的向量场                                │
│  ↓                                                       │
│  方法：将莫比乌斯带表示为 [0,1]×R 的商空间                  │
│  ↓                                                       │
│  商映射：(t,x) ~ (t+1, -x)                               │
│  ↓                                                       │
│  构造：在商空间上定义相容的向量场                          │
└─────────────────────────────────────────────────────────┘
```

#### 完整解决流程

**Step 1：莫比乌斯带的商空间模型**

莫比乌斯带 M 可表示为：
M = ([0, 1] × ℝ) / ~
其中 (0, x) ~ (1, -x)。

**Step 2：局部坐标卡**

- 卡1：U_1 = (0, 1) × ℝ，坐标 (t, x)
- 卡2：U_2 = ([0, 1/2) ∪ (1/2, 1]) × ℝ / ~

**Step 3：向量场构造**

在商空间上定义向量场：
V(t, x) = sin(2πt)·∂/∂t + x·cos(2πt)·∂/∂x

验证相容性：在重叠区域，利用 t ↦ t+1 和 x ↦ -x 变换，向量场保持不变。

**Step 4：零点分析**

零点满足：sin(2πt) = 0 且 x·cos(2πt) = 0。

解得：t = 0, 1/2（模1）且 x = 0。

但 t=0 和 t=1 在商空间中是同一点（经过 x ↦ -x 等同），所以只有一个零点在 [(0, 0)] = [(1, 0)]。

**Step 5：拓扑指数计算**

在零点处，线性化矩阵为：
DV = [[2π, 0], [0, 1]]

特征值为正，指数 ind(V, 0) = +1。

#### 验证与反思

**正确性验证：**
- ✅ 商空间模型正确：莫比乌斯带的拓扑由对径翻转识别给出
- ✅ 向量场相容性：验证变换下的不变性
- ✅ 零点唯一性：通过等同关系减少零点数量

**方法反思：**
> 商空间上的向量场构造需要仔细验证相容性条件。莫比乌斯带的非定向性使其上的向量场构造比环面复杂得多。

#### 相关变体

| 变体 | 问题 | 挑战 |
|:---:|:---|:---|
| 克莱因瓶 | 无零点向量场存在性 | 非定向闭曲面 |
| 射影平面 | 向量场零点下界 | 欧拉示性数为1 |
| 任意曲面 | 向量场与示性类关系 | Poincaré-Hopf定理 |

---

### 问题4：Grassmann流形 G(k, n) 的切空间刻画

#### 问题陈述

刻画Grassmann流形 G(k, n)（ℝ^n 中 k 维子空间的集合）在某点 Π ∈ G(k, n) 处的切空间 T_Π G(k, n)。

#### 问题类型识别

| 维度 | 类型 | 核心任务 |
|:---:|:---|:---|
| 对象类型 | 齐性空间 | 切空间的代数刻画 |
| 方法类型 | 群作用 | 利用李群结构 |
| 难度等级 | ⭐⭐⭐⭐⭐ | 高级理论问题 |

#### 方法选择决策

```
┌─────────────────────────────────────────────────────────┐
│  问题：G(k,n) 的切空间                                    │
│  ↓                                                       │
│  方法1：Plücker嵌入                                      │
│  方法2：商空间 O(n)/(O(k)×O(n-k))                        │
│  ↓                                                       │
│  选择方法2，利用齐性空间结构                              │
│  ↓                                                       │
│  结论：T_Π G(k,n) ≅ Hom(Π, Π^⊥)                          │
└─────────────────────────────────────────────────────────┘
```

#### 完整解决流程

**Step 1：Grassmann流形作为齐性空间**

G(k, n) = O(n) / (O(k) × O(n-k))

正交群 O(n) 传递地作用在 k 维子空间集合上。

**Step 2：切空间的群论刻画**

对于齐性空间 G/H，有：
T_[e](G/H) ≅ 𝔤/𝔥

其中 𝔤 = 𝔰𝔬(n)，𝔥 = 𝔰𝔬(k) ⊕ 𝔰𝔬(n-k)。

**Step 3：具体计算**

𝔰𝔬(n) 由反对称矩阵组成。分解为分块形式：
X = [[A, -B^T], [B, D]]

其中 A ∈ 𝔰𝔬(k)，D ∈ 𝔰𝔬(n-k)，B ∈ M_{(n-k)×k}(ℝ)。

商空间 𝔰𝔬(n)/𝔰𝔬(k)⊕𝔰𝔬(n-k) 由 B 参数化。

**Step 4：几何解释**

矩阵 B 对应线性映射 Π → Π^⊥：对 v ∈ Π，选择曲线 γ(t) 使得 γ(0) = v 且方向由 B 决定。

因此：
T_Π G(k, n) ≅ Hom(Π, Π^⊥)

维数：dim G(k, n) = k(n-k) = dim Hom(Π, Π^⊥) ✓

#### 验证与反思

**正确性验证：**
- ✅ 维数匹配：k(n-k) = k(n-k) ✓
- ✅ 几何直观：切方向对应子空间的"旋转"
- ✅ 特例验证：G(1, n) = ℝP^(n-1)，T_[v]ℝP^(n-1) ≅ v^⊥ ≅ Hom(span(v), v^⊥) ✓

**方法反思：**
> Grassmann流形是代数几何和微分几何的交汇点。齐性空间方法虽然抽象，但给出了切空间最清晰的刻画。Plücker嵌入提供了另一种外代数视角。

#### 相关变体

| 变体 | 问题 | 方法 |
|:---:|:---|:---|
| 复Grassmann流形 | G_ℂ(k, n) | 类似，使用 U(n) |
| 辛Grassmann流形 | Lagrangian Grassmannian | 辛群作用 |
| 旗流形 | 嵌套子空间族 | 推广上述方法 |

---

### 问题5：流形间的光滑映射：隐函数定理应用

#### 问题陈述

设 f: ℝ^3 → ℝ，f(x, y, z) = x^2 + y^2 - z^2。
- (a) 证明 M = f^(-1)(1) 是光滑流形
- (b) 求 M 在某点的切空间
- (c) 参数化 M 并计算诱导度量

#### 问题类型识别

| 维度 | 类型 | 核心任务 |
|:---:|:---|:---|
| 对象类型 | 正则值原像 | 隐函数定理应用 |
| 方法类型 | 微分判定 | 计算雅可比矩阵 |
| 难度等级 | ⭐⭐⭐ | 标准应用类问题 |

#### 方法选择决策

```
┌─────────────────────────────────────────────────────────┐
│  问题：双叶双曲面的光滑结构                                │
│  ↓                                                       │
│  方法：正则值定理 (Sard定理的推论)                         │
│  ↓                                                       │
│  验证：df ≠ 0 在 f^(-1)(1) 上                            │
│  ↓                                                       │
│  参数化：双曲坐标                                         │
└─────────────────────────────────────────────────────────┘
```

#### 完整解决流程

**Step 1：正则值验证**

计算微分 df = (2x, 2y, -2z)。

若 df = 0，则 x = y = z = 0，此时 f(0, 0, 0) = 0 ≠ 1。

因此 1 是 f 的正则值，M = f^(-1)(1) 是光滑 2 维流形。

**Step 2：切空间计算**

在点 p = (x_0, y_0, z_0) ∈ M 处：
T_pM = ker df_p = {v ∈ ℝ^3 : 2x_0·v_1 + 2y_0·v_2 - 2z_0·v_3 = 0}

即 T_pM 是梯度 ∇f(p) = (2x_0, 2y_0, -2z_0) 的正交补空间。

**Step 3：参数化**

对于 z > 0 的叶，使用双曲坐标：
x = cosh u · cos v, y = cosh u · sin v, z = sinh u

其中 u ∈ ℝ，v ∈ [0, 2π)。

验证：x^2 + y^2 - z^2 = cosh^2 u - sinh^2 u = 1 ✓

**Step 4：诱导度量**

计算切向量：
∂/∂u = (sinh u · cos v, sinh u · sin v, cosh u)
∂/∂v = (-cosh u · sin v, cosh u · cos v, 0)

第一基本形式：
E = ||∂/∂u||^2 = sinh^2 u + cosh^2 u = cosh(2u)
F = ∂/∂u · ∂/∂v = 0
G = ||∂/∂v||^2 = cosh^2 u

度量张量：
g = cosh(2u) du^2 + cosh^2 u dv^2

#### 验证与反思

**正确性验证：**
- ✅ 参数化有效性：雅可比矩阵秩为2
- ✅ 度量正定性：E > 0 且 EG - F^2 = cosh(2u)·cosh^2 u > 0
- ✅ 几何直观：M 是连通的双叶曲面，每叶微分同胚于 ℝ^2

**方法反思：**
> 正则值定理是构造流形的强大工具。关键在于验证微分的秩条件。双曲坐标的选择源于问题的对称性——这是处理二次曲面的一般策略。

#### 相关变体

| 变体 | 方程 | 结果 |
|:---:|:---|:---|
| 单叶双曲面 | x^2 + y^2 - z^2 = -1 | 连通，拓扑 S^1 × ℝ |
| 椭球面 | x^2 + y^2 + z^2 = 1 | 紧致流形 S^2 |
| 一般二次曲面 | x^T A x = 1 | 依赖 A 的符号 |

---

### 问题6：带边流形的边界坐标卡

#### 问题陈述

设 M = {(x, y, z) ∈ ℝ^3 : x^2 + y^2 + z^2 ≤ 1, z ≥ 0}（上半球体）。
- (a) 证明 M 是带边 3 维流形
- (b) 构造边界和内点的坐标卡
- (c) 计算边界的诱导定向

#### 问题类型识别

| 维度 | 类型 | 核心任务 |
|:---:|:---|:---|
| 对象类型 | 带边流形 | 边界处理 |
| 方法类型 | 半空间模型 | 标准坐标卡 |
| 难度等级 | ⭐⭐⭐ | 基础构造类问题 |

#### 方法选择决策

```
┌─────────────────────────────────────────────────────────┐
│  问题：带边流形的坐标卡                                    │
│  ↓                                                       │
│  模型：上半空间 H^n = {(x_1,...,x_n) : x_n ≥ 0}          │
│  ↓                                                       │
│  边界 ∂M = M ∩ {z = 0}  (闭圆盘)                         │
│  内部 int(M) = {x^2+y^2+z^2 < 1, z > 0}                  │
│  ↓                                                       │
│  坐标卡：球坐标限制 + 边界平坦化                          │
└─────────────────────────────────────────────────────────┘
```

#### 完整解决流程

**Step 1：内部坐标卡**

对于内部点 p = (x, y, z) 满足 x^2 + y^2 + z^2 < 1 且 z > 0，使用球坐标：
φ(x, y, z) = (ρ, θ, φ_球)

其中 ρ ∈ [0, 1)，θ ∈ [0, 2π)，φ_球 ∈ (0, π/2]。

这是到 ℝ^3 开集的光滑同胚。

**Step 2：边界坐标卡**

对于边界点 p = (x, y, 0) 满足 x^2 + y^2 < 1，需要映射到上半空间 H^3 = {(u, v, w) : w ≥ 0}。

定义：ψ(x, y, z) = (x, y, z)

这是恒等映射，将边界 {z = 0} 映射到 {w = 0}，内部 {z > 0} 映射到 {w > 0}。

**Step 3：边界流形结构**

∂M = {(x, y, 0) : x^2 + y^2 ≤ 1} 是闭圆盘，是带边 2 维流形。

**Step 4：诱导定向**

M 从 ℝ^3 继承标准定向，体积形式为 dx ∧ dy ∧ dz。

在边界上，外法向为 ν = (0, 0, -1)（指向下方，离开 M）。

诱导定向由 ι_ν(dx ∧ dy ∧ dz) = -dx ∧ dy 给出。

若以 (x, y) 为边界坐标，定向由 -dx ∧ dy 或等价地 dy ∧ dx 给出。

#### 验证与反思

**正确性验证：**
- ✅ 内部坐标卡：标准球坐标，光滑性显然
- ✅ 边界坐标卡：恒等映射的限制，满足带边流形定义
- ✅ 定向相容性：斯托克斯定理要求边界定向与内部定向相容

**方法反思：**
> 带边流形的坐标卡构造关键在于"拉平"边界。上半空间模型 H^n 是标准工具。诱导定向的符号约定需要特别注意——不同文献可能使用不同的外法向定义。

#### 相关变体

| 变体 | 对象 | 坐标卡策略 |
|:---:|:---|:---|
| 实心环面 | T^2 × I | 柱坐标限制 |
| 流形的柄体 | 亏格 g 的柄体 | Morse理论视角 |
| 单纯复形 | 几何实现 | 分段线性结构 |


---

## 类型2：曲率计算问题

### 问题7：平面曲线的曲率与Frenet标架

#### 问题陈述

设 γ(t) = (t, t^2) 为抛物线参数化。
- (a) 计算弧长参数化
- (b) 求曲率 κ(s)
- (c) 绘制曲率函数并分析几何意义

#### 问题类型识别

| 维度 | 类型 | 核心任务 |
|:---:|:---|:---|
| 对象类型 | 平面曲线 | 曲率计算 |
| 方法类型 | Frenet公式 | 弧长参数化 |
| 难度等级 | ⭐⭐ | 基础计算类问题 |

#### 方法选择决策

```
┌─────────────────────────────────────────────────────────┐
│  问题：抛物线的曲率                                       │
│  ↓                                                       │
│  方法1：弧长参数化后直接计算                               │
│  方法2：一般参数公式 κ = |γ' × γ''| / |γ'|^3              │
│  ↓                                                       │
│  选择方法2（计算更直接）                                   │
└─────────────────────────────────────────────────────────┘
```

#### 完整解决流程

**Step 1：计算导数**

γ'(t) = (1, 2t), γ''(t) = (0, 2)

**Step 2：弧长参数化**

弧长微元：
ds/dt = |γ'(t)| = √(1 + 4t^2)

弧长函数：
s(t) = ∫_0^t √(1 + 4τ^2) dτ = t√(1+4t^2)/2 + (1/4)sinh^(-1)(2t)

逆函数 t(s) 没有初等闭式。

**Step 3：曲率计算（一般参数公式）**

对于平面曲线 γ(t) = (x(t), y(t))：
κ = |x'y'' - x''y'| / (x'^2 + y'^2)^(3/2)

计算：
- x' = 1, x'' = 0
- y' = 2t, y'' = 2
- x'y'' - x''y' = 1·2 - 0·2t = 2

因此：
κ(t) = 2 / (1 + 4t^2)^(3/2)

**Step 4：曲率分析**

- 在 t = 0（顶点）：κ_max = 2
- 当 |t| → ∞：κ → 0
- 曲率关于 t=0 对称

几何意义：抛物线在顶点处弯曲最剧烈，远离顶点时逐渐变平。

#### 验证与反思

**正确性验证：**
- ✅ 量纲分析：曲率 ∼ 1/长度，公式给出 ∼ 1/t 量纲 ✓
- ✅ 极限行为：远离顶点曲率趋于0符合直观
- ✅ 与圆的比较：若曲率恒为 2，则半径为 1/2 的圆

**方法反思：**
> 一般参数曲率公式避免了求弧长参数化的困难。抛物线的曲率在顶点最大，这一性质使其成为逼近曲面的重要工具（抛物点逼近）。

#### 相关变体

| 变体 | 曲线 | 曲率特点 |
|:---:|:---|:---|
| 椭圆 | (a·cos t, b·sin t) | 顶点处极值，四点共轭 |
| 悬链线 | (t, cosh t) | 曲率与 y^2 成反比 |
| 摆线 | 圆滚动的轨迹 | 拱形曲率变化 |

---

### 问题8：曲面的高斯曲率：悬链面

#### 问题陈述

悬链面是悬链线绕轴旋转得到的曲面，参数化为：
X(u, v) = (cosh u · cos v, cosh u · sin v, u)

计算其第一、第二基本形式及高斯曲率 K。

#### 问题类型识别

| 维度 | 类型 | 核心任务 |
|:---:|:---|:---|
| 对象类型 | 旋转曲面 | 高斯曲率计算 |
| 方法类型 | 直接计算 | 第一、第二基本形式 |
| 难度等级 | ⭐⭐⭐ | 标准计算类问题 |

#### 方法选择决策

```
┌─────────────────────────────────────────────────────────┐
│  问题：悬链面的高斯曲率                                    │
│  ↓                                                       │
│  方法：第一、第二基本形式 → 形状算子 → 高斯曲率              │
│  ↓                                                       │
│  K = det(II) / det(I) = (LN - M^2) / (EG - F^2)          │
│  ↓                                                       │
│  期望：K ≤ 0（极小曲面的特征）                             │
└─────────────────────────────────────────────────────────┘
```

#### 完整解决流程

**Step 1：切向量计算**

X_u = (sinh u · cos v, sinh u · sin v, 1)
X_v = (-cosh u · sin v, cosh u · cos v, 0)

**Step 2：第一基本形式**

E = X_u · X_u = sinh^2 u + 1 = cosh^2 u
F = X_u · X_v = 0
G = X_v · X_v = cosh^2 u

所以：
I = cosh^2 u · du^2 + cosh^2 u · dv^2

**Step 3：单位法向量**

X_u × X_v = (-cosh u · cos v, -cosh u · sin v, sinh u · cosh u)

|X_u × X_v| = cosh u · √(cosh^2 u + sinh^2 u) = cosh u · √cosh(2u)

单位法向量：
N = (-cos v, -sin v, sinh u) / √cosh(2u)

**Step 4：第二基本形式**

计算二阶导数：
X_uu = (cosh u · cos v, cosh u · sin v, 0)
X_uv = (-sinh u · sin v, sinh u · cos v, 0)
X_vv = (-cosh u · cos v, -cosh u · sin v, 0)

第二基本形式系数：
L = X_uu · N = -cosh u / √cosh(2u)
M = X_uv · N = 0
N = X_vv · N = cosh u / √cosh(2u)

**Step 5：高斯曲率**

K = (LN - M^2) / (EG - F^2) = (-cosh^2 u / cosh(2u)) / cosh^4 u = -1 / (cosh^2 u · cosh(2u))

利用 cosh(2u) = 2·cosh^2 u - 1：
K = -1 / (cosh^2 u · (2·cosh^2 u - 1)) < 0

#### 验证与反思

**正确性验证：**
- ✅ 符号：K < 0 处处成立，符合双曲型曲面特征
- ✅ 极小曲面验证：平均曲率 H = (1/2)·(EN + GL - 2FM)/(EG-F^2) = (1/2)·(cosh^3 u - cosh^3 u)/(...) = 0 ✓
- ✅ 渐近行为：u → ∞ 时 K → 0

**方法反思：**
> 悬链面是唯一的旋转极小曲面。高斯曲率的负号反映了曲面的鞍形特征——沿经线方向弯曲向下，沿纬线方向弯曲向上。

#### 相关变体

| 变体 | 曲面 | 高斯曲率 |
|:---:|:---|:---|
| 球面 | S^2 | K = 1/R^2 > 0 |
| 圆柱 | S^1 × ℝ | K = 0 |
| 伪球面 | 曳物线旋转面 | K = -1（常负） |

---

### 问题9：Riemann曲率张量计算：S^2 的标准度量

#### 问题陈述

设 S^2 ⊂ ℝ^3 为半径为 R 的球面，配备诱导度量。
- (a) 计算 Christoffel 符号 Γ^k_ij
- (b) 计算 Riemann 曲率张量 R^l_ijk
- (c) 计算 Ricci 曲率 R_ij 和标量曲率 R

#### 问题类型识别

| 维度 | 类型 | 核心任务 |
|:---:|:---|:---|
| 对象类型 | 常曲率空间 | 张量计算 |
| 方法类型 | 直接计算 | 协变导数定义 |
| 难度等级 | ⭐⭐⭐⭐ | 高级计算类问题 |

#### 方法选择决策

```
┌─────────────────────────────────────────────────────────┐
│  问题：球面的曲率张量                                      │
│  ↓                                                       │
│  坐标选择：球坐标 (θ, φ)                                  │
│  ↓                                                       │
│  度量：g = R^2 dθ^2 + R^2 sin^2 θ dφ^2                   │
│  ↓                                                       │
│  Christoffel → Riemann → Ricci → Scalar                  │
└─────────────────────────────────────────────────────────┘
```

#### 完整解决流程

**Step 1：度量张量**

球坐标参数化：
g_ij = [[R^2, 0], [0, R^2·sin^2 θ]], g^ij = [[R^(-2), 0], [0, R^(-2)·sin^(-2) θ]]

**Step 2：Christoffel 符号**

公式：Γ^k_ij = (1/2)·g^kl·(∂_i g_jl + ∂_j g_il - ∂_l g_ij)

非零分量：
- Γ^θ_φφ = -sin θ·cos θ
- Γ^φ_θφ = Γ^φ_φθ = cot θ

其余为零。

**Step 3：Riemann 曲率张量**

公式：R^l_ijk = ∂_j Γ^l_ik - ∂_k Γ^l_ij + Γ^l_jm·Γ^m_ik - Γ^l_km·Γ^m_ij

独立非零分量：
R^θ_φθφ = sin^2 θ, R^φ_θφθ = -1

降指标：
R_θφθφ = g_θθ·R^θ_φθφ = R^2·sin^2 θ

**Step 4：Ricci 曲率**

R_ij = R^k_ikj

计算：
R_θθ = R^φ_θφθ = 1
R_φφ = R^θ_φθφ = sin^2 θ

即：
R_ij = (1/R^2)·g_ij

**Step 5：标量曲率**

R = g^ij·R_ij = 1/R^2 + 1/R^2 = 2/R^2

#### 验证与反思

**正确性验证：**
- ✅ Einstein 流形：R_ij = (R/2)·g_ij = (1/R^2)·g_ij ✓
- ✅ 维数匹配：2维流形，R = 2K 其中 K = 1/R^2 是高斯曲率 ✓
- ✅ 对称性：Riemann 张量的所有对称性都满足

**方法反思：**
> 球面是常正曲率空间的典范。Einstein 流形条件 R_ij = λ·g_ij 反映了高度对称性。计算中注意：2维情况下 Riemann 张量只有一个独立分量。

#### 相关变体

| 变体 | 空间 | 曲率特征 |
|:---:|:---|:---|
| 双曲平面 | ℍ^2 | 常负曲率 K = -1/R^2 |
| 平坦环面 | T^2 | R_ijkl = 0 |
| 复射影空间 | ℂP^n | Fubini-Study度量 |

---

### 问题10：测地线方程：双曲平面的测地线

#### 问题陈述

上半平面模型 ℍ^2 = {(x, y) : y > 0} 配备庞加莱度量：
ds^2 = (dx^2 + dy^2) / y^2

- (a) 计算测地线方程
- (b) 证明测地线是垂直于 x 轴的半圆或直线

#### 问题类型识别

| 维度 | 类型 | 核心任务 |
|:---:|:---|:---|
| 对象类型 | 双曲空间 | 测地线计算 |
| 方法类型 | Euler-Lagrange | 变分法 |
| 难度等级 | ⭐⭐⭐⭐ | 高级计算类问题 |

#### 方法选择决策

```
┌─────────────────────────────────────────────────────────┐
│  问题：双曲平面的测地线                                    │
│  ↓                                                       │
│  方法1：Christoffel符号 + 测地线方程                       │
│  方法2：变分法（Euler-Lagrange）                           │
│  方法3：等距不变性（利用对称性）                            │
│  ↓                                                       │
│  选择方法2（计算更直接）                                   │
└─────────────────────────────────────────────────────────┘
```

#### 完整解决流程

**Step 1：拉格朗日量**

能量泛函的被积函数：
L = (ẋ^2 + ẏ^2) / y^2

其中 ẋ = dx/dt，ẏ = dy/dt。

**Step 2：Euler-Lagrange 方程**

对于 x：
d/dt(∂L/∂ẋ) = d/dt(2ẋ/y^2) = 2ẍ/y^2 - 4ẋẏ/y^3

∂L/∂x = 0

方程：ẍ - 2ẋẏ/y = 0

对于 y：
d/dt(∂L/∂ẏ) = 2ÿ/y^2 - 4ẏ^2/y^3

∂L/∂y = -2(ẋ^2 + ẏ^2)/y^3

方程：ÿ + (ẋ^2 - ẏ^2)/y = 0

**Step 3：首次积分**

拉格朗日量不显含 x，故守恒量：
p_x = ∂L/∂ẋ = 2ẋ/y^2 = c_1

若 c_1 = 0，则 ẋ = 0，即垂直线 x = const。

若 c_1 ≠ 0：
ẋ = c_1·y^2/2

**Step 4：求解轨迹**

利用能量守恒（以弧长参数化）：
(ẋ^2 + ẏ^2)/y^2 = 1

代入 ẋ = c_1·y^2/2：
(c_1^2·y^4/4 + ẏ^2)/y^2 = 1

ẏ^2 = y^2 - c_1^2·y^4/4 = y^2(1 - c_1^2·y^2/4)

分离变量并求解，消去参数后得：
(x - x_0)^2 + y^2 = 4/c_1^2

这是中心在 (x_0, 0)、半径为 2/|c_1| 的圆，在上半平面的部分。

#### 验证与反思

**正确性验证：**
- ✅ 垂直直线：令 c_1 → ∞ 的极限情形
- ✅ 垂直于边界：圆心都在 y=0 上，故与边界垂直
- ✅ 等距不变性：分式线性变换保持此类曲线

**方法反思：**
> 双曲几何的核心特征是"直线"（测地线）与欧氏几何不同。垂直于边界的圆弧是双曲平面的"直线"，这一性质使双曲几何具有丰富的三角形性质（内角和小于 π）。

#### 相关变体

| 变体 | 模型 | 测地线 |
|:---:|:---|:---|
| 庞加莱圆盘 | D^2 | 正交于边界的圆弧 |
| Klein模型 | D^2 | 欧氏直线（弦） |
| 双曲空间 | ℍ^n | 正交于边界的半圆/直线 |

---

### 问题11：高斯-博内定理应用：曲面的拓扑限制

#### 问题陈述

设 S 是 ℝ^3 中的紧致连通曲面，亏格为 g。
- (a) 陈述高斯-博内定理
- (b) 若 K > 0 处处成立，证明 S ≅ S^2
- (c) 若 K = 0 处处成立，证明 S 是平坦环面

#### 问题类型识别

| 维度 | 类型 | 核心任务 |
|:---:|:---|:---|
| 对象类型 | 紧致曲面 | 整体拓扑性质 |
| 方法类型 | 积分几何 | 曲率-拓扑联系 |
| 难度等级 | ⭐⭐⭐⭐⭐ | 高级理论问题 |

#### 方法选择决策

```
┌─────────────────────────────────────────────────────────┐
│  问题：曲率对拓扑的限制                                    │
│  ↓                                                       │
│  核心：高斯-博内定理                                       │
│  ∫_S K dA = 2π χ(S) = 2π(2 - 2g)                         │
│  ↓                                                       │
│  分析：曲率符号 → 欧拉示性数符号 → 亏格限制                 │
└─────────────────────────────────────────────────────────┘
```

#### 完整解决流程

**Step 1：高斯-博内定理**

对于无边紧致定向黎曼曲面 (S, g)：
∫_S K dA = 2π χ(S)

其中 χ(S) = 2 - 2g 是欧拉示性数，g 是亏格。

**Step 2：正曲率情形**

假设 K > 0 处处成立，则：
∫_S K dA > 0

因此 χ(S) > 0。

对于闭曲面，χ(S) = 2 - 2g > 0 意味着 g = 0。

亏格为0的定向闭曲面微分同胚于 S^2。

**Step 3：平坦情形分析**

假设 K = 0 处处成立，则：
∫_S K dA = 0 = 2π χ(S)

因此 χ(S) = 0，即 g = 1。

**Step 4：平坦环面的存在性**

需要证明：任何亏格1的曲面都可配备平坦度量。

构造：S = ℝ^2 / Λ，其中 Λ 是格点群。商度量是平坦的。

反之，任何配备平坦度量的紧致曲面都是 T^2。

#### 验证与反思

**正确性验证：**
- ✅ 球面：χ(S^2) = 2，可配备 K = 1 的度量 ✓
- ✅ 环面：χ(T^2) = 0，标准平坦度量 K = 0 ✓
- ✅ 高亏格：χ < 0，必须 K 变号或恒负

**方法反思：**
> 高斯-博内定理是微分几何中最深刻的结果之一，它建立了局部几何量（曲率）与整体拓扑量（欧拉示性数）的联系。这是Chern-Weil理论的原型。

#### 相关变体

| 变体 | 情形 | 结论 |
|:---:|:---|:---|
| 带边曲面 | ∂S ≠ ∅ | 需要边界项 ∫_{∂S} k_g ds |
| 非紧曲面 | 非紧完备 | Cohn-Vossen不等式 |
| 高维推广 | 2n 维流形 | Gauss-Bonnet-Chern定理 |

---

### 问题12：形状算子与主曲率：椭球面

#### 问题陈述

设椭球面 E: x^2/a^2 + y^2/b^2 + z^2/c^2 = 1，其中 a > b > c > 0。
- (a) 在 (x, 0, z) 处计算形状算子
- (b) 求主曲率和主方向
- (c) 分析脐点（umbilic points）的位置

#### 问题类型识别

| 维度 | 类型 | 核心任务 |
|:---:|:---|:---|
| 对象类型 | 二次曲面 | 形状算子分析 |
| 方法类型 | 隐式曲面法 | Weingarten映射 |
| 难度等级 | ⭐⭐⭐⭐ | 高级计算类问题 |

#### 方法选择决策

```
┌─────────────────────────────────────────────────────────┐
│  问题：椭球面的形状算子                                    │
│  ↓                                                       │
│  方法：隐式曲面 F(x,y,z) = x^2/a^2 + y^2/b^2 + z^2/c^2 - 1│
│  ↓                                                       │
│  梯度 ∇F 给出法向，Hessian D^2F 与形状算子相关              │
│  ↓                                                       │
│  主曲率 = 形状算子的特征值                                  │
└─────────────────────────────────────────────────────────┘
```

#### 完整解决流程

**Step 1：隐式表示**

F(x, y, z) = x^2/a^2 + y^2/b^2 + z^2/c^2 - 1 = 0

梯度（外法向）：
∇F = (2x/a^2, 2y/b^2, 2z/c^2)

**Step 2：参数化与第一基本形式**

在 (x, 0, z) 附近参数化（y = 0 平面）：
y = ±b·√(1 - x^2/a^2 - z^2/c^2)

取 y > 0 分支，切平面由基向量张成。

**Step 3：形状算子**

形状算子 S = -dN 满足 ⟨S(X), Y⟩ = II(X, Y)。

对于隐式曲面，形状算子在切向量 v 上的作用：
S(v) = (1/|∇F|)·(I - (∇F ⊗ ∇F)/|∇F|^2)·D^2F(v)

**Step 4：主曲率计算**

在点 (x, 0, z)，由对称性，x 和 z 方向是主方向。

计算得主曲率与 a, b, c 以及点的位置有关。

**Step 5：脐点分析**

脐点满足 κ_1 = κ_2。

这需要复杂的代数分析。结果表明：椭球面有4个脐点，位于 y = 0 平面上的曲率线交点处。

具体位置满足：
x^2/(a^2(a^2-b^2)) = z^2/(c^2(b^2-c^2))

#### 验证与反思

**正确性验证：**
- ✅ 球面特例：a=b=c=R 时，所有点都是脐点，κ_1 = κ_2 = 1/R ✓
- ✅ 对称性：脐点在 x-z 平面上成对出现
- ✅ 数量：4个脐点符合Jacobi关于3轴椭球面的经典结果

**方法反思：**
> 椭球面是研究曲面理论的试验场。脐点的分布反映了曲面的内在对称性。4个脐点是最小数量，这与Poincaré-Hopf定理关于向量场零点的结论相关。

#### 相关变体

| 变体 | 曲面 | 脐点特征 |
|:---:|:---|:---|
| 环面 | 旋转环面 | 无脐点（除退化情形） |
| 三轴环面 | 一般旋转面 | 0、2或4个脐点 |
| Dupin四次圆纹曲面 | 特殊曲面 | 所有点都是脐点（球面） |

---

### 问题13：指数映射与法坐标：局部几何

#### 问题陈述

设 (M, g) 是黎曼流形，p ∈ M。
- (a) 定义指数映射 exp_p: T_pM → M
- (b) 证明 exp_p 在 0 ∈ T_pM 处是局部微分同胚
- (c) 在法坐标下计算度量张量的Taylor展开

#### 问题类型识别

| 维度 | 类型 | 核心任务 |
|:---:|:---|:---|
| 对象类型 | 黎曼流形 | 指数映射理论 |
| 方法类型 | 测地线变分 | 局部坐标计算 |
| 难度等级 | ⭐⭐⭐⭐⭐ | 高级理论问题 |

#### 方法选择决策

```
┌─────────────────────────────────────────────────────────┐
│  问题：指数映射与法坐标                                    │
│  ↓                                                       │
│  定义：exp_p(v) = γ_v(1)，其中 γ_v 是从 p 出发初速为 v 的测地线│
│  ↓                                                       │
│  关键性质：d(exp_p)_0 = id（由测地线方程）                 │
│  ↓                                                       │
│  应用：法坐标使度量在 p 处标准欧氏化                       │
└─────────────────────────────────────────────────────────┘
```

#### 完整解决流程

**Step 1：指数映射定义**

对于 v ∈ T_pM，设 γ_v 是满足：
- γ_v(0) = p
- γ_v'(0) = v

的唯一测地线。

定义：exp_p(v) = γ_v(1)（当 γ_v 定义到 t=1 时）。

**Step 2：局部微分同胚性**

由测地线方程的性质：
γ_{tv}(s) = γ_v(ts)

因此：exp_p(tv) = γ_v(t)

在 t=0 处对 t 求导：
d/dt|_{t=0} exp_p(tv) = γ_v'(0) = v

这说明 d(exp_p)_0(v) = v，即 d(exp_p)_0 = id_{T_pM}。

由反函数定理，exp_p 在 0 的某邻域内是微分同胚。

**Step 3：法坐标**

选择 T_pM 的标准正交基 {e_1, ..., e_n}，定义法坐标：
(x^1, ..., x^n) ↦ exp_p(x^i·e_i)

**Step 4：度量张量的Taylor展开**

在法坐标下，度量张量满足：
- g_ij(0) = δ_ij
- ∂_k g_ij(0) = 0（法坐标的性质）

二阶项与曲率相关：
g_ij(x) = δ_ij + (1/3)·R_ikjl(p)·x^k·x^l + O(|x|^3)

**Step 5：体积元展开**

√det g = 1 - (1/6)·R_ij(p)·x^i·x^j + O(|x|^3)

其中 R_ij 是Ricci曲率。

#### 验证与反思

**正确性验证：**
- ✅ 维数1：M = ℝ，exp_p(v) = p + v ✓
- ✅ 维数2：S^2 上，exp_p 将切平面映射到球面，沿测地大圆
- ✅ 曲率联系：展开式中的曲率项反映了局部几何

**方法反思：**
> 法坐标是黎曼几何的"标准模型"。在法坐标下，流形在一点附近看起来最像欧氏空间。曲率出现在度量的二阶展开中，这是曲率作为"二阶几何量"的精确含义。

#### 相关变体

| 变体 | 对象 | 应用 |
|:---:|:---|:---|
| 正规坐标 | 沿曲线的标架平移 | 计算Jacobi场 |
| 凸邻域 | 指数映射的像 | 唯一测地线存在性 |
| 比较几何 | 曲率比较 | Toponogov定理 |

