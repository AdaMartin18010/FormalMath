# Nakayama引理 - 五种表征

**创建日期**: 2025年12月1日
**领域**: 交换代数
**难度**: L2

---

## 元信息

| 属性 | 内容 |
|------|------|
| **定理** | Nakayama引理 |
| **领域** | 交换代数 |
| **发现者** | Tadashi Nakayama |
| **前置知识** | 模、局部环、Jacobson根 |

---

## 一、符号表征（形式化）

### 1.1 严格陈述

**Nakayama引理**：设R是局部环，𝔪是极大理想，M是有限生成R-模。若𝔪M = M，则M = 0。

等价地：若M/𝔪M = 0，则M = 0。

---

## 二、几何表征（可视化）

```text
模M在"无穷小层面"为0 ⟹ M本身为0

    𝔪M = M ⟹ M = 0
```

---

## 三、直觉表征（类比）

> **Nakayama**：如果模的"一阶近似"为零，则模本身为零

---

## 四、计算表征（算法）

```python
def nakayama_check(M, m_ideal):
    # 如果 m·M = M，则 M = 0
    quotient = M / (m_ideal * M)
    return quotient == 0
```

---

## 五、范畴表征（抽象）

Nakayama引理是局部环上模论的基础，用于证明局部生成→全局生成。

---

**状态**: ✅ 完成
