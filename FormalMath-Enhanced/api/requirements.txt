# FormalMath API 依赖
# 安装: pip install -r requirements.txt

# FastAPI核心
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# 数据库
sqlalchemy>=2.0.0
alembic>=1.12.0
aiosqlite>=0.19.0  # 异步SQLite支持

# Redis缓存
redis>=5.0.0
hiredis>=2.2.0  # Redis C解析器加速

# Celery异步任务
celery>=5.3.0
kombu>=5.3.0
billiard>=4.2.0

# 响应压缩
brotli>=1.1.0

# HTTP客户端（测试用）
httpx>=0.25.0

# 性能监控
psutil>=5.9.0

# 开发依赖（可选）
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
mypy>=1.5.0
