# FormalMath 系统集成问题修复清单

**生成时间**: 2026-04-04T16:41:56  
**问题总数**: 14  
**严重问题**: 13 (依赖缺失)  
**一般问题**: 1 (代码问题)

---

## 修复清单概览

### 依赖安装命令

```bash
# 进入API目录
cd FormalMath-Enhanced/api

# 安装所有依赖
pip install -r requirements.txt

# 或使用测试依赖
pip install -r ../../tests/backend/requirements-test.txt
```

### 具体依赖列表

| 依赖包 | 版本要求 | 用途 | 安装命令 |
|--------|----------|------|----------|
| sqlalchemy | >=2.0.0 | 数据库ORM | `pip install sqlalchemy>=2.0.0` |
| redis | >=5.0.0 | 缓存服务 | `pip install redis>=5.0.0` |
| fastapi | >=0.104.0 | API框架 | `pip install fastapi>=0.104.0` |
| uvicorn | >=0.24.0 | ASGI服务器 | `pip install uvicorn[standard]>=0.24.0` |
| pydantic | >=2.0.0 | 数据验证 | `pip install pydantic>=2.0.0` |
| pydantic-settings | >=2.0.0 | 配置管理 | `pip install pydantic-settings>=2.0.0` |

---

## 详细修复项

### 1. 数据库引擎初始化

- **类别**: 数据库连接
- **错误**: `No module named 'sqlalchemy'`
- **状态**: 🔴 未修复

**修复步骤**:
```bash
pip install sqlalchemy>=2.0.0 alembic>=1.12.0 aiosqlite>=0.19.0
```

**验证命令**:
```python
python -c "import sqlalchemy; print(sqlalchemy.__version__)"
```

---

### 2. 数据库表创建

- **类别**: 数据库连接
- **错误**: `No module named 'sqlalchemy'`
- **状态**: 🔴 未修复

**修复步骤**: 同第1项

---

### 3. 数据库索引创建

- **类别**: 数据库连接
- **错误**: `No module named 'sqlalchemy'`
- **状态**: 🔴 未修复

**修复步骤**: 同第1项

---

### 4. 数据库会话管理

- **类别**: 数据库连接
- **错误**: `No module named 'sqlalchemy'`
- **状态**: 🔴 未修复

**修复步骤**: 同第1项

---

### 5. Redis缓存初始化

- **类别**: 缓存系统
- **错误**: `No module named 'redis'`
- **状态**: 🔴 未修复

**修复步骤**:
```bash
pip install redis>=5.0.0 hiredis>=2.2.0
```

**验证命令**:
```python
python -c "import redis; print(redis.__version__)"
```

---

### 6. Redis缓存基本操作

- **类别**: 缓存系统
- **错误**: `No module named 'redis'`
- **状态**: 🔴 未修复

**修复步骤**: 同第5项

---

### 7. Redis缓存业务方法

- **类别**: 缓存系统
- **错误**: `No module named 'redis'`
- **状态**: 🔴 未修复

**修复步骤**: 同第5项

---

### 8. FastAPI应用创建

- **类别**: API端点
- **错误**: `No module named 'fastapi'`
- **状态**: 🔴 未修复

**修复步骤**:
```bash
pip install fastapi>=0.104.0 uvicorn[standard]>=0.24.0
```

**验证命令**:
```python
python -c "import fastapi; print(fastapi.__version__)"
```

---

### 9. API路由注册

- **类别**: API端点
- **错误**: `No module named 'fastapi'`
- **状态**: 🔴 未修复

**修复步骤**: 同第8项

---

### 10. 知识图谱API

- **类别**: API端点
- **错误**: `No module named 'fastapi'`
- **状态**: 🔴 未修复

**修复步骤**: 同第8项

---

### 11. 学习路径API

- **类别**: API端点
- **错误**: `No module named 'fastapi'`
- **状态**: 🔴 未修复

**修复步骤**: 同第8项

---

### 12. 搜索API

- **类别**: API端点
- **错误**: `No module named 'fastapi'`
- **状态**: 🔴 未修复

**修复步骤**: 同第8项

---

### 13. 健康检查API

- **类别**: API端点
- **错误**: `No module named 'fastapi'`
- **状态**: 🔴 未修复

**修复步骤**: 同第8项

---

### 14. 概念提取工具

- **类别**: 工具模块
- **错误**: `cannot import name 'extract_concepts_from_yaml'`
- **状态**: 🟡 未修复

**修复步骤**:
检查 `tools/extract_concepts.py` 文件，确认导出函数名称。

**可能的解决方案**:
1. 修改导入语句，使用正确的函数名
2. 或者修改 `extract_concepts.py` 添加导出函数

**检查命令**:
```python
python -c "from extract_concepts import *; print(dir())"
```

---

## 修复进度跟踪

| 序号 | 问题 | 优先级 | 状态 | 负责人 | 完成日期 |
|------|------|--------|------|--------|----------|
| 1-4 | 数据库依赖 | P0 | ⬜ 待修复 | - | - |
| 5-7 | Redis依赖 | P0 | ⬜ 待修复 | - | - |
| 8-13 | FastAPI依赖 | P0 | ⬜ 待修复 | - | - |
| 14 | 函数名修复 | P1 | ⬜ 待修复 | - | - |

---

## 一键修复脚本

创建文件 `fix_dependencies.ps1`:

```powershell
# FormalMath 依赖修复脚本

Write-Host "正在安装 FormalMath 依赖..." -ForegroundColor Green

# 安装数据库依赖
pip install sqlalchemy>=2.0.0 alembic>=1.12.0 aiosqlite>=0.19.0

# 安装Redis依赖
pip install redis>=5.0.0 hiredis>=2.2.0

# 安装FastAPI依赖
pip install fastapi>=0.104.0 uvicorn[standard]>=0.24.0

# 安装其他依赖
pip install pydantic>=2.0.0 pydantic-settings>=2.0.0
pip install celery>=5.3.0 kombu>=5.3.0
pip install brotli>=1.1.0 httpx>=0.25.0 psutil>=5.9.0

Write-Host "依赖安装完成！" -ForegroundColor Green
Write-Host "请重新运行集成测试验证修复结果。" -ForegroundColor Yellow
```

运行脚本:
```powershell
.\fix_dependencies.ps1
```

---

## 验证修复

修复完成后，运行以下命令验证:

```bash
# 重新运行集成测试
cd tests
python integration/system_integration_test.py

# 或运行原始测试套件
pytest backend/ -v
```

---

## 联系支持

如修复过程中遇到问题，请检查:
1. Python版本是否 >= 3.10
2. pip是否为最新版本: `pip install --upgrade pip`
3. 网络连接是否正常
4. 虚拟环境是否正确激活
