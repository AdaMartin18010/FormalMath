# 参考文献系统修复进展报告

**任务编号**: P2-Ref
**报告日期**: 2026-04-17
**负责人**: AI Agent (Kimi Code CLI)

---

## 1. 背景与目标

P1-T4 审计结果显示，项目文档中 **DOI、arXiv ID、zbMATH、MathSciNet MR Number 的检出率均为 0%**。为彻底解决该问题，P2-Ref 任务组立即启动了参考文献 YAML 元数据标准化与批量修复工作。

本阶段目标：

1. 建立统一的 `references` YAML frontmatter 规范。
2. 开发不依赖外部非标准库的批量注入脚本。
3. 预置常用数学教材与数据库的 YAML 模板库。
4. 选取核心文档进行试点修复。
5. 输出可审计的进展报告。

---

## 2. 已交付物

### 2.1 规范性文件（3 个）

| 文件路径 | 说明 |
|----------|------|
| `project/00-参考文献YAML元数据规范.md` | 定义了 `textbook`、`paper`、`database` 三类条目的完整字段标准，包含 6 个不同数学分支的完整 frontmatter 示例，明确了强制/可选字段。 |
| `scripts/reference_templates.yaml` | 预置了 6 本经典教材（Rudin、Hartshorne、Strang、Munkres、Dummit & Foote、Hardy & Wright）及 6 个数据库（nLab、Stacks Project、MathWorld、zbMATH、MathSciNet、arXiv）的 YAML 片段库。采用 JSON-compatible 语法，确保纯标准库即可解析。 |
| `output/00-参考文献系统修复进展报告.md` | 本文档。 |

### 2.2 可运行脚本（1 个）

| 文件路径 | 说明 |
|----------|------|
| `scripts/batch_add_references.py` | 纯 Python 标准库实现的批量参考文献注入工具。功能包括：扫描现有 frontmatter、缺失时插入 `references` 模板、支持从 `reference_templates.yaml` 按 key 批量注入、具备 `--dry-run` 安全预览模式、不破坏现有内容。 |

**脚本用法示例**：

```bash
# Dry-run 预览
python scripts/batch_add_references.py --dir docs/03-分析学/01-实分析/ --inject rudin_pma,nlab --dry-run

# 实际注入
python scripts/batch_add_references.py --dir docs/03-分析学/01-实分析/ --inject rudin_pma,nlab
```

---

## 3. 试点文档修复详情

选取 `docs/03-分析学/01-实分析/` 目录下 **7 篇核心文档** 作为试点，使用脚本注入基础模板后，手动补充了 **Rudin *Principles of Mathematical Analysis* 的精确章节引用**。

| 序号 | 文档路径 | 注入模板 | Rudin 精确章节引用 |
|------|----------|----------|--------------------|
| 1 | `01-实分析.md` | `rudin_pma`, `nlab` | Ch 1, 2, 3, 4, 5, 6, 7, 11 |
| 2 | `01-实分析-习题补充.md` | `rudin_pma`, `nlab` | Ch 1, 3 |
| 3 | `01-实分析-增强版.md` | `rudin_pma`, `nlab` | Ch 1, 2, 3, 4, 5, 6, 7, 11 |
| 4 | `01-实分析-深度扩展版.md` | `rudin_pma`, `nlab` | Ch 1, 2, 3, 4, 5, 6, 7, 11 |
| 5 | `02-Lebesgue积分-深度扩展版.md` | `rudin_pma`, `nlab` | Ch 1, 2, 11 |
| 6 | `一致收敛与函数项级数-深度版.md` | `rudin_pma`, `nlab` | Ch 7 |
| 7 | `实数完备性等价定理-深度版.md` | `rudin_pma`, `nlab` | Ch 1, Appendix to Ch 1 (Dedekind Cuts) |

所有试点文档的 `references` 字段均符合 `project/00-参考文献YAML元数据规范.md` 中的标准格式，包含完整的教材元数据（ISBN、MSC、出版社、版次、年份）及数据库链接模板。

---

## 4. 质量检查

- [x] 脚本仅使用 Python 标准库（`argparse`、`json`、`pathlib`、`sys` 等），无外部依赖。
- [x] 所有试点文档均已通过 `batch_add_references.py` 的 `--dry-run` 预检，确认无 frontmatter 破坏风险。
- [x] 注入后的 frontmatter 语法正确，`chapters` 列表缩进符合 YAML 规范。
- [x] 7 篇试点文档均包含 Rudin 的精确章节引用。

---

## 5. 问题与改进

**问题发现**：`01-实分析-增强版.md` 与 `01-实分析-深度扩展版.md` 原文件存在**双 frontmatter**（第一个 `---` 块后紧接着第二个 `---` 块）。脚本在第一个 frontmatter 中正确插入了 `references`，未破坏后续结构。建议后续对全库进行 frontmatter 质量审计，修复双 frontmatter 问题。

**改进建议**：

1. 将 `batch_add_references.py` 集成到 CI 流程中，对新提交的 Markdown 文件自动检查 `references` 完整性。
2. 扩展 `reference_templates.yaml`，加入更多中文教材与近年重要论文模板。
3. 开发基于该规范的静态站点生成器插件，在渲染页面时自动生成引用卡片和外部链接。

---

## 6. 下一步行动计划

| 步骤 | 内容 | 目标时间 |
|------|------|----------|
| 6.1 | 将 `batch_add_references.py` 推广至 `docs/` 全目录，分批注入空 references 模板 | 2026-04-24 |
| 6.2 | 组织人工审核，为各学科核心文档补充精确章节/论文 DOI/arXiv ID | 2026-05-01 |
| 6.3 | 运行自动化检出脚本，验证 DOI / arXiv / zbMATH / MR Number 检出率是否提升至目标值（≥80%） | 2026-05-08 |
| 6.4 | 更新项目级 `AGENTS.md`，将参考文献规范纳入 agent 写作指南 | 2026-05-08 |

---

## 7. 结论

P2-Ref 任务第一阶段已顺利完成：

- **3 个规范性文件** 已就位；
- **1 个可运行的 Python 脚本** 已验证；
- **7 篇试点文档** 已更新规范的 `references` 字段并包含 Rudin 精确章节引用；
- 本报告已归档至 `output/00-参考文献系统修复进展报告.md`。

参考文献系统从零到一的基础框架已经搭建完成，具备了向全项目推广的条件。
